# Copy this file to `.env.local` and fill in your Supabase project values for local development.
# Do NOT commit the real values. `.env.local` is in .gitignore.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=REPLACE_ME_ANON_KEY
